from setuptools import setup, find_packages

setup(
    name="ipag-gin-cfexplainer",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Add your dependencies from requirements.txt here
        # or leave empty and use requirements.txt separately
    ],
    python_requires='>=3.7',
)